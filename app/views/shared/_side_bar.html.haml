.chat-side
  .side-header
    %p.side-header__user-name 
      = current_user.name
      %ul.side-header__lists
        %li.list
          = link_to new_group_path do
            = fa_icon 'edit', class: 'header__box__menu__new-group'
        %li.list
          = link_to edit_user_path(current_user) do
            = fa_icon 'cog', class: 'header__box__menu__edit-user'
  .groups
    - current_user.groups.each do |group|
    .group
      = link_to '#' do
      %p.group__name 
        = group.name
      %p.group__message 
        メッセージはまだありません。
